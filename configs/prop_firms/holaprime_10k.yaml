label: "Hola Direct $10K"
description: >-
  Hola Direct $10K — trailing EOD max loss, 20% consistency (USD engine).
engine: usd
max_loss_mode: trailing_eod
consistency_pct: 20
consistency_rule: hola_best_day
initial_balance_usd: 10000
reference_usd:
  daily_loss: 300
  max_loss: 700
  risk_per_trade: 150
  consistency_pct: 20
daily_loss_pct: 3.0
max_drawdown_pct: 7.0
allowed_instruments:
  - BTC_PERP
  - ETH_PERP
