description: >-
  Hola Prime Direct $10K (approx % rules). Full USD caps, consistency, and SL/TP
  parity — coming in a later bot update (Phase B).
daily_loss_pct: 3.0
max_drawdown_pct: 7.0
allowed_instruments:
  - BTC_PERP
  - ETH_PERP
