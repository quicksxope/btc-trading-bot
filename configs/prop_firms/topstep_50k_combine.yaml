label: "TopStep Combine $50K"
description: >-
  TopStep $50K Combine — trailing MLL $2,000 (locks at $50k), DLL $1,000, target $3,000, 50% consistency.
engine: usd
max_loss_mode: trailing_eod
mll_lock_at_initial: true
consistency_rule: topstep_target_ratio
consistency_target_ratio: 0.5
daily_loss_pct: 2.0
max_drawdown_pct: 4.0
profit_target_pct: 6.0
initial_balance_usd: 50000
reference_usd:
  profit_target: 3000
  daily_loss: 1000
  max_loss: 2000
execution_defaults:
  mode: sltp_risk
  risk_per_trade_usd: 500
  risk_reward_ratio: 2.0
  swing_lookback: 5
allowed_instruments:
  - NASDAQ_CFD
  - SPX500_CFD
  - XAUUSD_CFD
  - BTC_PERP
  - ETH_PERP
