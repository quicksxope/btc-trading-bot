label: "Hola Direct $50K"
description: >-
  Hola Direct $50K funded-style — trailing EOD max loss $3,500, daily $1,500, 20% consistency.
engine: usd
max_loss_mode: trailing_eod
consistency_pct: 20
consistency_rule: hola_best_day
daily_loss_pct: 3.0
max_drawdown_pct: 7.0
initial_balance_usd: 50000
reference_usd:
  daily_loss: 1500
  max_loss: 3500
  risk_per_trade: 750
execution_defaults:
  mode: sltp_risk
  risk_per_trade_usd: 750
  risk_reward_ratio: 2.0
  swing_lookback: 5
  max_trades_per_day: 2
allowed_instruments:
  - BTC_PERP
  - ETH_PERP
