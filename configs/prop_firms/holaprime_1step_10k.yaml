label: "Hola 1-Step Prime $10K"
description: >-
  Hola Prime 1-Step $10K — USD engine, static max loss 6%, target 10%.
engine: usd
max_loss_mode: static
initial_balance_usd: 10000
reference_usd:
  profit_target: 1000
  daily_loss: 300
  max_loss: 600
  risk_per_trade: 200
daily_loss_pct: 3.0
max_drawdown_pct: 6.0
profit_target_pct: 10.0
min_trading_days: 2
execution_defaults:
  mode: sltp_risk
  risk_per_trade_usd: 200
  risk_reward_ratio: 2.0
  swing_lookback: 5
  max_trades_per_day: 3
allowed_instruments:
  - BTC_PERP
  - ETH_PERP
